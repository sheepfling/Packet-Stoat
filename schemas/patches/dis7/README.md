# DIS7 schema patches

Place fastdis-owned DIS7 schema corrections and generation annotations here.

Do not hand-edit generated outputs to encode schema fixes. Record them here and
flow them back through generation.
