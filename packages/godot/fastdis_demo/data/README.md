Generate a replay file for the demo here, for example:

```bash
python tools/make_replay.py packages/godot/fastdis_demo/data/synthetic.fastdispkt --packets 2048 --entities 3
```

The demo scene looks for `res://data/synthetic.fastdispkt` by default.
